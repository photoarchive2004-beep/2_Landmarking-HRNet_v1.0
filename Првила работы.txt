Мы работаем над проектом 2_Landmarking-Yolo_v1.0 — модуль авторазметки ландмарок по YOLO.

Единственные источники истины:
- ТЗ_YOLO_v1.0 (файл ТЗ-YOLO.docx, я его сейчас прикреплю).
- Репозиторий GitHub: https://github.com/photoarchive2004-beep/2_Landmarking-Yolo_v1.0 (ветка main).
- Плюс те логи и файлы, которые я отдельно приложу в чат.

Строгие правила:
1) Перед любыми советами или кодом — сначала ознакомься с ТЗ и актуальными файлами репозитория.
2) Если какого-то файла/лога ты не видишь, прямо попроси меня его прислать. Не фантазируй содержимое.
3) Не придумывай новые папки, форматы, режимы, если этого нет в ТЗ.
4) Никогда не проси меня править файлы руками или вводить команды по одной. Для любых изменений и запусков давай полный PowerShell-скрипт, который:
   - переходит в D:\GM\tools\2_Landmarking-Yolo_v1.0,
   - делает нужные правки (создаёт/обновляет файлы),
   - при необходимости запускает Python-скрипты,
   - выполняет git add/commit/push origin main,
   - пишет Start-Transcript / Stop-Transcript в logs\...
5) Команды git reset --hard, push --force и любые операции, переписывающие историю, запрещены.
6) Если git push не удался, честно напиши, что репозиторий НЕ синхронизирован, и попроси меня прислать лог.
7) Если тебе не хватает информации, сначала спроси, а не додумывай.

Сначала:
- прочитай ТЗ-YOLO.docx;
- изучи структуру репозитория;
- составь план работ в виде нумерованного списка задач (без кода).
